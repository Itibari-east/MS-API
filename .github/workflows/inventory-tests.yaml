name: Inventory Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      environment:
        description: Target Env
        type: choice
        options: [PRODUCTION, DEV]
        default: PRODUCTION

jobs:
  run-playwright-tests:
    uses: ./.github/workflows/itibari-reusable.yaml
    with:
      tag: "@inventory"
      moduleName: "Inventory API"
      moduleSlug: inventory
      environment: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'PRODUCTION' }}
      shardIndex: 1
      shardTotal: 1
      node-version: 20
      use-cache: true
    secrets:
      MS_USER_EMAIL: ${{ secrets.MS_USER_EMAIL }}
      MS_USER_PASSWORD: ${{ secrets.MS_USER_PASSWORD }}
      MS_TOTP_SECRET: ${{ secrets.MS_TOTP_SECRET }}
      MS_DEV_BASE_URL: ${{ secrets.MS_DEV_BASE_URL }}

  publish-report:
    name: Publish Inventory report
    runs-on: ubuntu-latest
    if: always()
    needs: run-playwright-tests
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      PLAYWRIGHT_MODULE_NAME: Inventory API
      PLAYWRIGHT_ENVIRONMENT: ${{ github.event_name == 'workflow_dispatch' && inputs.environment || 'PRODUCTION' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download shard artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: blob-report-inventory-*
          path: all-blob-reports
          merge-multiple: true
          if-no-files-found: warn

      - name: Verify Slack webhook
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "::error::SLACK_WEBHOOK_URL is not available in this job"
            exit 1
          fi

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Merge Playwright reports
        run: |
          mkdir -p test-results
          npx playwright merge-reports --reporter html ./all-blob-reports
          npx playwright merge-reports --reporter json ./all-blob-reports > test-results/playwright-report.json

      - name: Publish Slack summary
        run: npm run report:slack -- test-results/playwright-report.json

      - name: Upload merged report
        uses: actions/upload-artifact@v4
        with:
          name: inventory-playwright-report
          path: |
            playwright-report/
            test-results/playwright-report.json
          if-no-files-found: warn
